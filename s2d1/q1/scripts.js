
let theme = 'dark';

let data = document.getElementById('data');
data.innerText = theme;

let toogler = (theme) => {
    if(theme === 'dark'){
        console.log(theme)
    }else{
        console.log(theme)
    }
}

// toogler();


console.log(toogler());
